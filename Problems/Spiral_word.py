def spiral_word(word):
    #Checking no duplicates
    if len(set(word)) != len(word):
        return False
    for i in
            print("very nice")


print(spiral_word("urgent"))

#for i in range(len(word) - 1): if word[] > word[i + 1]: return True else:return False
